features.fusion_of_multimodality_features
=========================================

.. py:module:: features.fusion_of_multimodality_features

.. autoapi-nested-parse::

   Fuse multimodality features into a single machine learning dataset.

   Considering that the number of features may differ among the modalities
   and that some features may be missing for some files, use the following
   heuristic strategy to concatenate features creating a multimodal row:
   1) Fuse:
       1.1) For each group of participant_id, session_id, time_date, count how
       many features are available for each modality. The largest number Nmax will
       dictate how many rows will be created for that group. For example, if for
       a given group there are Nmax=6 features for PPG, 3 for ECG and 1 for bioimpedance,
       then Nmax=6 rows will be created for that group, with the available features for
       each modality.
       1.2) Create each row such that all modalities contribute with Nmax features,
       by repeating features as needed. For example, if for a given group there are
       Nmax=6 features for PPG, 3 for ECG and 1 for bioimpedance, then the 3 ECG features
       will be repeated twice and the 1 bioimpedance feature will be repeated 6 times to
       create 6 rows for that group.
       1.3) Count and warn (indicate via stdout) the case in which a group has no features
       for a modality, and skip that group, since it will not contribute to the machine
       learning model.
   2) Save a single DataFrame csv with fused features and all available metadata
      (participant_id, session_id, time_date, etc.) for all files.



Attributes
----------

.. autoapisummary::

   features.fusion_of_multimodality_features.parser


Functions
---------

.. autoapisummary::

   features.fusion_of_multimodality_features.get_feature_columns_for_modality
   features.fusion_of_multimodality_features.get_metadata_columns
   features.fusion_of_multimodality_features.old_get_feature_and_metadata_columns
   features.fusion_of_multimodality_features.old_clean_by_removing_nan
   features.fusion_of_multimodality_features.rename_metadata_columns_to_care_for_modality
   features.fusion_of_multimodality_features.load_modality_dataframes
   features.fusion_of_multimodality_features.old_get_feature_columns
   features.fusion_of_multimodality_features.repeat_features
   features.fusion_of_multimodality_features.fuse_modality_features


Module Contents
---------------

.. py:function:: get_feature_columns_for_modality(df_modality: pandas.DataFrame, metadata_columns: List[str]) -> List[str]

   Get feature columns (excluding metadata) and metadata columns for specific modality.


.. py:function:: get_metadata_columns(datasetConfig: datasets_util.naming_conventions.DatasetConfig) -> List[str]

   Get metadata columns.


.. py:function:: old_get_feature_and_metadata_columns(datasetConfig: datasets_util.naming_conventions.DatasetConfig, df_ecg: pandas.DataFrame, df_ppg: pandas.DataFrame, df_bioimp: pandas.DataFrame) -> Tuple[List[str], List[str]]

   Get feature columns (excluding metadata) and metadata columns.


.. py:function:: old_clean_by_removing_nan(df: pandas.DataFrame, modality: str) -> pandas.DataFrame

   Clean a modality-specific dataframe by removing rows/columns with NaN/inf values.
   Drop columns that are not relevant for ML.


.. py:function:: rename_metadata_columns_to_care_for_modality(datasetConfig: datasets_util.naming_conventions.DatasetConfig, df: pandas.DataFrame, modality: str) -> pandas.DataFrame

   Rename metadata columns to avoid conflicts.


.. py:function:: load_modality_dataframes(datasetConfig: datasets_util.naming_conventions.DatasetConfig) -> Tuple[pandas.DataFrame, pandas.DataFrame, pandas.DataFrame]

.. py:function:: old_get_feature_columns(df: pandas.DataFrame) -> List[str]

   Get feature columns (excluding metadata).


.. py:function:: repeat_features(df: pandas.DataFrame, n_times: int, feature_cols: List[str]) -> pandas.DataFrame

   Repeat rows n_times by cycling through them.


.. py:function:: fuse_modality_features(modality_frames: Dict[str, pandas.DataFrame], required_modalities: List[str], metadata_columns: List[str]) -> pandas.DataFrame

   Concatenate features from required modalities using a single strategy.


.. py:data:: parser

